# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
# also ignore any .env file at the project root in case someone
# adds one without the .local suffix — never want secrets in git
.env

# Office / Word temp lock files
~$*

# raw working assets / scratch — keep these on disk locally,
# don't ship them with the repo. Real production assets live in
# /public/images.
/Aman_Kailakarn/
/form_pagesource.txt
/form_pagesource.docx

# editor / OS noise
Thumbs.db
.idea/
.vscode/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
